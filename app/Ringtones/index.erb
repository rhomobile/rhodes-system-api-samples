<div>
	<% if @ringtones.empty? %>
		No ring tones
	<% else %>
		<% @ringtones.each do |k,v| %>
			<%= link_to k, { :action => 'play', :query => { :name => k, :file => v } } %> <br/>
		<% end %>
	<% end %>
</div>
